{
  "name": "arewevalid",
  "version": "1.0.0",
  "description": "The most tiny and easy to use jquery form validation plugin",
  "main": "arewevalid.min.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sakib",
  "license": "MIT",
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-contrib-jshint": "^0.11.3",
    "grunt-contrib-uglify": "^0.10.1",
    "grunt-contrib-watch": "^0.6.1"
  }
}
